---
const {
  id = "modalCursos",
  title = "Cursos disponibles",
  children,
} = Astro.props;
---

<div id={id} class="modal hidden">
  <div class="modal-content">
    <span id={`${id}-close`} class="close">X</span>
    <h2>{title}</h2>
    <slot />
  </div>
</div>

<style>
  .modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.6);
    display: flex;
    justify-content: center;
    align-items: center;
  }
  .hidden {
    display: none;
  }
  .modal-content {
    background: white;
    padding: 2rem;
    border-radius: 10px;
    width: 300px;
    text-align: center;
    position: relative;
  }
  .close {
    position: absolute;
    top: 15px;
    right: 20px;
    font-size: 1.5rem;
    cursor: pointer;
  }

  /*Contenido del modal
  se usa :global para aplicar estilos globales a los elementos del modal
  ya que estan en otro archivo los elementos ul / li*/

  :global(.modal-content ul) {
    list-style: none;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin: 1rem 0;
  }

  :global(.modal-content li a) {
    display: block;
    padding: 0.75rem 1rem;
    background: linear-gradient(156.25deg, #ab4afd, #482d8a);
    color: #fff;
    text-decoration: none;
    border-radius: 10px;
    font-weight: 600;
    text-align: center;
    transition:
      transform 0.2s,
      background 0.3s;
  }

  :global(.modal-content li a:hover) {
    transform: scale(1.05);
    background: linear-gradient(156.25deg, #b969ff, #24014a);
  }
</style>
